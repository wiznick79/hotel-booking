jwt:
  secret: test-secret-that-is-long-enough-for-hmac-sha256

spring:
  datasource:
    url: jdbc:h2:mem:identity-test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    admin:
      fail-fast: false

identity-events:
  topics:
    auto-create: false
