spring:
  datasource:
    url: jdbc:h2:mem:booking-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

jwt:
  secret: test-secret-that-is-long-enough-for-hmac-sha256

guest-access:
  encryption-secret: test-guest-access-encryption-secret

booking:
  outbox:
    dispatch-delay-ms: 3600000
